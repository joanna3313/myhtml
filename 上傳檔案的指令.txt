# =註解

git status
=檢查狀態

git add
= (檔名) 將未被追蹤的檔案，索引區內的檔案添加到暫存區

git commit  -m "上傳檔案的名稱"
=給予上傳檔案名稱
*記得前後要加雙引號
*檔名盡量以英文命名(但中文也可以)

git push
=上傳到GitHub，現在預設分支為mastr

git push origin master/main 
= 當然也可以推到main分支，但是要指定

在Vscode打html指令，可以輸入開頭指令後按tab鍵，將會自動帶出尾端指令
*Ex：key h2+tab，自動帶出h2
